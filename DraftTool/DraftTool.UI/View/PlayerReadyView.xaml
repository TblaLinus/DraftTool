<UserControl x:Class="DraftTool.UI.View.PlayerReadyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Center" FontSize="50" FontWeight="Bold" Text="{Binding ReadyText}"/>
            <Button Width="300" Height="100" Margin="100" Command="{Binding ReadyCommand}">
                <TextBlock FontSize="40" FontWeight="Bold">Ready</TextBlock>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>

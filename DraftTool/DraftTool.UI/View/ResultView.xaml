<UserControl x:Class="DraftTool.UI.View.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DraftTool.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="DimGray"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListBox VerticalAlignment="Center" HorizontalAlignment="Center" Width="1500" Margin="20" BorderThickness="0" Background="{DynamicResource BackgroundBrush}" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding ResultDeck}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Image VerticalAlignment="Center" Height="200" Source="{Binding ImageURL}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="1" Width="200" Height="50" Margin="20" Command="{Binding DoneCommand}">Done</Button>

    </Grid>
</UserControl>
